<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tatneft_quest">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name="com.example.tatneft_quest.fragments.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TatneftQuest">
        <activity android:name="com.example.tatneft_quest.travelPackage.LocationHistoryFragment" />
        <activity android:name="com.example.tatneft_quest.firstActivity.RegistrationActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name="com.example.tatneft_quest.firstActivity.RestorePasswordActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name="com.example.tatneft_quest.firstActivity.AuthorizationActivity"
            android:windowSoftInputMode="stateHidden"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${GOOGLE_MAPS_API_KEY}" />

        <service
            android:name="com.example.tatneft_quest.services.LocationService"
            android:enabled="true"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" /> <!-- <activity -->
        <!-- android:name="com.journeyapps.barcodescanner.CaptureActivity" -->
        <!-- android:screenOrientation="fullSensor" -->
        <!-- tools:replace="screenOrientation" /> -->
        <activity
            android:name="com.example.tatneft_quest.firstActivity.SplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.tatneft_quest.firstActivity.FirstScreenActivity"
            android:screenOrientation="portrait" />

        <activity android:name="com.example.tatneft_quest.MainActivity" />
        <activity
            android:name="com.example.tatneft_quest.slider.SliderActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>